# Absolute origin of the deployed site, with protocol and no trailing slash.
# Feeds canonical URLs, sitemap.xml, robots.txt and Open Graph tags — set this
# before shipping, or crawlers will be handed localhost.
NEXT_PUBLIC_SITE_URL=https://jutsu.dev
